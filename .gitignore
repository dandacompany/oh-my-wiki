# except the published tutorial showcase + the full-feature reference)
# Meta/planning + generated tutorials: docs/ is local-only, never distributed
docs/

# Claude Code / AI agent instructions and memory (local-only, never publish)
CLAUDE.md
CLAUDE.local.md
AGENTS.md
GEMINI.md
MEMORY.md
memory/
.claude/

# Secrets / env
.env
.env.*
!.env.example

# IDE / editor config
.vscode/
.idea/
*.swp

# Python
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Runtime state (per-user sqlite)
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
data/*.migrated
!data/.gitkeep

# Virtualenv
.venv/

# Editable install metadata
*.egg-info/

# Build artifacts (sdist/wheel + the build-time bundle copy — never committed)
build/
dist/
scripts/_bundle/

# OS
.DS_Store

# uv (not used here)
uv.lock

# Per-vault runtime state (autoresearch sessions, hot cache state, etc.)
.oh-my-wiki/

# humanize-korean transient workspace
_workspace/

# Skills CLI local install artifacts (a self-copy of the skill + lockfile —
# `skills add` writes these; redundant with the repo source, never committed)
.agents/
skills-lock.json

# Understand-Anything analysis artifacts (local only)
.ua/
.understand-anything/
